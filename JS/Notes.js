/*

GEC: The Global Execution Context is the default or base execution context in which JavaScript code runs when the script starts.
It is created when your JavaScript file or script begins execution and is responsible for managing the global code
(i.e., code that is not inside any function or block).


*/

class Node {
  constructor(val) {
    this.val = val;
    this.next = null;
  }
}


class Linkedlist{
    constructor(){
        this.head
    }
}